{
  "name": "web-game-experiments",
  "version": "1.0.0",
  "description": "A collection of web game experiments",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "eleventy --serve",
    "start&build": "bun run build && eleventy --serve",
    "build:gol": "bun build src/game-of-life/main.ts --outdir docs/game-of-life/ --target browser",
    "build": "bun run build:gol && cp src/game-of-life/index.html docs/game-of-life/index.html && cp style.css docs/ && eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "latest",
    "@types/node": "25.6.0",
    "typescript": "6.0.3"
  },
  "dependencies": {
    "pixi.js": "8.18.1"
  }
}